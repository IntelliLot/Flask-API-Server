# Simple Video Feed Server Configuration

# Camera Configuration
CAMERA_INDEX=0                              # Camera device (0 for default camera)

# Remote Server Configuration  
REMOTE_SERVER_URL=http://localhost:5000/upload  # Where to send frames

# Capture Configuration
CAPTURE_INTERVAL=5                          # Seconds between captures

# Optional: Video file instead of camera
# VIDEO_FILE=path/to/video.mp4